# Retired Results

This folder stores rejected or retired experiment outputs that we do not want
mixed into the active professor-facing showcase.

Current subfolders:

- `clahe_rejected/`
  CLAHE and histogram-equalization related side results that were judged worse
  than the main homomorphic-filter story.
- `postprocessing_rejected/`
  Older archived post-processing outputs from earlier project decisions.
