# Design Document - Homomorphic Filtering Project

## Project Objective

Evaluate how effectively homomorphic filtering can correct non-uniform illumination, and study how filter design choices (Butterworth vs Gaussian, parameter settings) influence the balance between illumination suppression and detail preservation.

## Theory Summary

- Image model: `f(x, y) = i(x, y) * r(x, y)`
- Pipeline: `ln -> DFT -> H(u, v) filter -> IDFT -> exp`
- Filter parameters: `gamma_L` (< 1, compresses illumination), `gamma_H` (> 1, boosts reflectance), `D0` (cutoff frequency), `c` (sharpness, Butterworth only), `n` (filter order, Butterworth only)

## Implementation Status

| Script                      | Description                                                      | Status          |
| --------------------------- | ---------------------------------------------------------------- | --------------- |
| 01_create_synthetic.py      | Generate synthetic uneven illumination test image from cameraman | [x] Completed   |
| 02_homomorphic_grayscale.py | Core homomorphic filtering pipeline for grayscale images         | [x] Completed   |
| 03_parameter_sweep.py       | Sweep gamma_L, gamma_H, D0 and generate comparison grids         | [x] Completed   |
| 04_compare_with_heq.py      | Compare homomorphic filtering vs histogram equalization          | [x] Completed   |
| 05_color_hsi.py             | HSI color space extension for color images                       | [x] Prepared, waiting for real photos |
| 06_metrics.py               | Compute PSNR, SSIM on synthetic test case                        | [x] Completed   |

## Parameter Ranges to Test

- `gamma_L`: `[0.3, 0.5, 0.7, 0.9]`
- `gamma_H`: `[1.2, 1.5, 1.8, 2.0, 2.5]`
- `D0`: `[10, 20, 30, 50, 80]`
- Filter types: Butterworth (order 2), Gaussian
- For Butterworth: also vary order `n` in `[1, 2, 4]`

## Dataset

| Image                | Source                 | Type      | Purpose                                      |
| -------------------- | ---------------------- | --------- | -------------------------------------------- |
| cameraman.tif        | Course homework        | Grayscale | Base for synthetic test                      |
| synthetic_uneven.png | Generated by script 01 | Grayscale | Quantitative evaluation (ground truth known) |
| rice.png             | MATLAB / standard      | Grayscale | Classic non-uniform illumination benchmark   |
| photo_1.jpg          | Self-captured          | Color     | Real-world test, window lighting             |
| photo_2.jpg          | Self-captured          | Color     | Real-world test, desk lamp                   |
| photo_3.jpg          | Self-captured          | Color     | HSI extension + real-world test              |

## Evaluation Criteria

### Qualitative (all images)

- Reduction of illumination non-uniformity
- Visibility of details in dark and bright regions
- Preservation of edges and textures
- Avoidance of over-enhancement, halo artifacts, and unnatural appearance
- Histogram shape before vs after

### Quantitative (synthetic image only)

- PSNR (Peak Signal-to-Noise Ratio)
- SSIM (Structural Similarity Index)

## Decisions Log

<!-- Append every implementation decision, bug, tuning change, and visualization update here with a date. -->

- [2026-04-07] - First implementation pass is grayscale-first because only grayscale test images are currently available locally.
- [2026-04-07] - Implemented the grayscale scripts first: synthetic-image generation, homomorphic restoration, parameter sweep, histogram-equalization comparison, and synthetic metrics.
- [2026-04-07] - Initial homomorphic defaults (`gamma_L=0.5`, `gamma_H=1.5`, `D0=30`, Butterworth order 2) produced a restoration that looked acceptable visually but degraded PSNR and SSIM on the synthetic test.
- [2026-04-07] - Treated the metric drop as a tuning bug and adjusted the fixed restoration defaults to a milder setting: `gamma_L=0.6`, `gamma_H=1.1`, `D0=100`, Butterworth order 2.
- [2026-04-07] - Verified the tuned restoration improved over the synthetic input: corrupted image PSNR/SSIM = `13.35 dB / 0.8280`, restored image PSNR/SSIM = `17.89 dB / 0.9019`.
- [2026-04-07] - Added a PNG copy of the baseline `cameraman.tif` as `images/cameraman.png` to make quick visual inspection easier in editors that preview PNG more comfortably than TIF.
- [2026-04-07] - Added a dedicated side-by-side visualization `results/baseline_synthetic_restored.png` so the baseline, synthetic input, and restored output can be compared directly without relying only on the metric summary layout.
- [2026-04-07] - Reworked the synthetic corruption stage to support multiple paper-style illumination patterns and switched the default corruption from a mild Gaussian field to a stronger rotated ramp so the uneven lighting is easier to see by eye.
- [2026-04-07] - Added `results/paper_style_illumination_patterns.png` to document the vertical, sine-wave, and 45 degree rotated illumination fields referenced by the paper.
- [2026-04-07] - After increasing the synthetic corruption strength, retuned the default grayscale homomorphic filter to `gamma_L=0.3`, `gamma_H=1.1`, `D0=150`, Butterworth order 2 so restoration quality stays stronger on the new synthetic test.
- [2026-04-08] - Implemented the HSI color pipeline structure in script 05 and made it exit cleanly when the real color photos are not present yet.
- [2026-04-08] - Added blind results-table exports, a padding/windowing experiment script, and a final grayscale demo-pack script so the project has report-ready grayscale outputs before the real photo stage.
- [2026-04-10] - Added a dedicated flashlight demo script using the newly added `images/flashlight.jpeg` so the project now includes a real grayscale scene with strong flashlight illumination even before the full color photo set is collected.
- [2026-04-10] - Added a dedicated tunnel demo script using `images/tun.jpg` with a balanced blind restoration and a stronger display-oriented blind restoration so tunnel-style shadow lifting can be demonstrated quickly without expensive parameter searches.
- [2026-04-10] - Added a post-enhanced tunnel output on top of the blind homomorphic result so the tunnel demo can separate the effect of the blind filter itself from the extra effect of contrast stretching and gamma-based display enhancement.

## Known Issues

<!-- Track bugs, edge cases, things to fix -->

- [2026-04-07] - `05_color_hsi.py` is still intentionally incomplete until real color test photos are added.
